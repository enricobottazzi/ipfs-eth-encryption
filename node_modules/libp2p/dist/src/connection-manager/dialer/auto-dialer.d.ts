import type { PeerInfo } from '@libp2p/interfaces/peer-info';
import type { Components } from '@libp2p/interfaces/components';
export interface AutoDialerInit {
    enabled: boolean;
    minConnections: number;
    dialTimeout: number;
}
export declare class AutoDialer {
    private readonly components;
    private readonly enabled;
    private readonly minConnections;
    private readonly dialTimeout;
    constructor(components: Components, init: AutoDialerInit);
    handle(evt: CustomEvent<PeerInfo>): void;
}
//# sourceMappingURL=auto-dialer.d.ts.map