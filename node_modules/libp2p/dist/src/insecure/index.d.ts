import type { PeerId } from '@libp2p/interfaces/peer-id';
import type { ConnectionEncrypter, SecuredConnection } from '@libp2p/interfaces/connection-encrypter';
import type { Duplex } from 'it-stream-types';
export declare class Plaintext implements ConnectionEncrypter {
    protocol: string;
    secureInbound(localId: PeerId, conn: Duplex<Uint8Array>, remoteId?: PeerId): Promise<SecuredConnection>;
    secureOutbound(localId: PeerId, conn: Duplex<Uint8Array>, remoteId: PeerId): Promise<SecuredConnection>;
}
//# sourceMappingURL=index.d.ts.map